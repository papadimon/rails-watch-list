<%= cl_image_tag("ffigki9xiacnrdecdcbr", width: 400, height: 300, crop: :fill) %>

<%= cl_image_tag("qdmvii3diepwnsfa0dxf", width: 150, height: 150, crop: :thumb, gravity: :face) %>

<h1>Lists</h1>
<ul>
  <% @lists.each do |list| %>
    <li><%= link_to list.name, list_path(list) %></li>
  <% end %>
</ul>
<%= link_to "Add Movie List", new_list_path(@list), class: "btn btn-primary" %>

<%=  %></div>
